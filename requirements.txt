# Pinned to a known-good environment (torch/librosa/demucs combos are brittle
# across versions). Refresh with `.venv/bin/python -m pip freeze` after a verified
# upgrade. Dev-only tools (formatters, coverage) live in requirements-dev.txt.
flask==3.1.3
demucs==4.0.1
torch==2.12.1
torchcodec==0.14.0
librosa==0.11.0
matplotlib==3.11.0
numpy==2.4.6
scipy==1.18.0
pillow==12.2.0
soundfile==0.14.0
mlx-whisper==0.4.3; platform_system == "Darwin" and platform_machine == "arm64"
faster-whisper==1.2.1
# YouTube breaks old versions constantly (403s on the direct stream URLs that
# --download-sections hands to ffmpeg) — keep this fresh; `pip install -U yt-dlp`
# is the first fix to try on any YouTube download error.
yt-dlp>=2026.7.4
psycopg[binary]==3.3.4
# Local image generation (the Image gen / AI Stylize cards). Imported lazily —
# the app runs fine without them; generation just reports it's unavailable.
diffusers==0.39.0
transformers==5.13.0
safetensors==0.8.0
accelerate==1.14.0
# Control-image extraction (canny/soft-edge) for the Extract card, and the img2img
# preprocessing in AI Stylize. Headless build — no GUI/Qt deps.
opencv-python-headless==5.0.0
# HTTP client for the optional remote-inference mode (backend/remote_client.py posts
# diffusion work to a rented GPU running backend/remote_app.py).
requests==2.34.2
