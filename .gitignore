.venv/
data/
__pycache__/
*.pyc
.DS_Store

node_modules/
frontend/dist/
static/

# Tooling caches
.pytest_cache/
.ruff_cache/

# Env
.env

# Machine-specific captured fixtures (embed a local job_id; see fixtures/README.md)
*.local.json

# Coverage
htmlcov/
.coverage
coverage/

# Claude Code: the shared permission allowlist is checked in (.claude/settings.json);
# settings.local.json is per-machine — it accumulates one-off command approvals and
# absolute paths, so it is noise in a diff and wrong on anyone else's checkout.
.claude/settings.local.json
