# Dev-only tooling (lint, format, tests, coverage) — not needed to run the app.
# Install on top of requirements.txt:
#   .venv/bin/python -m pip install -r requirements-dev.txt
# Then: make lint · make test · make coverage · make format
ruff==0.15.19
pytest==9.1.1
pytest-cov
black
pre-commit
